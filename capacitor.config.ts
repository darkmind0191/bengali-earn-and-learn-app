import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darkmind.earnlearnbangla',
  appName: 'Earn & Learn বাংলা',
  webDir: 'dist'
};

export default config;
